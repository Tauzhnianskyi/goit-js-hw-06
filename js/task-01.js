const itemEl = document.querySelectorAll(".item");
console.log("Number of categories:", itemEl.length);

const itemTitle = document.querySelectorAll(".item");

console.log(itemTitle);

const body = document.body;
console.log(body);
